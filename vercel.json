{
  "env": {
    "PAYLOAD_BASE_URL": "@payload_base_url",
    "PAYLOAD_API_KEY": "@payload_api_key"
  },
  "build": {
    "env": {
      "PAYLOAD_BASE_URL": "@payload_base_url",
      "PAYLOAD_API_KEY": "@payload_api_key"
    }
  }
}
